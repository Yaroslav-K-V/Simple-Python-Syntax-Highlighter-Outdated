name: Python Simple Syntax Highlither

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Check syntax
      run: |
        python -m compileall .

    - name: Run application (Qt offscreen)
      env:
        QT_QPA_PLATFORM: offscreen
      run: |
        python - <<EOF
        import main
        print("Main imported successfully")
        EOF

